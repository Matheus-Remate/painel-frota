'use client';
import { AlertTriangle, ChevronRight, X } from 'lucide-react';
import { useState } from 'react';
export default function VehicleActivityHistory({ checkins }: { checkins: any[] }) { const [selected, setSelected] = useState<any>(null); return <div><h3 className="mb-4 text-xl font-bold text-white">Histórico de Atividades</h3><div className="space-y-4">{checkins?.length ? checkins.map((checkin) => <button key={checkin.id} type="button" onClick={() => setSelected(checkin)} className="flex w-full gap-4 rounded-xl border border-slate-700/50 bg-slate-800/30 p-4 text-left hover:bg-slate-800/60"><div className={`mt-1 w-2 self-stretch rounded-full ${checkin.has_issues ? 'bg-amber-500' : 'bg-emerald-500'}`} /><div className="flex-1"><div className="flex justify-between gap-4"><span className="font-medium text-slate-200">{checkin.has_issues ? 'Devolução com apontamentos' : 'Devolução regular'}</span><span className="text-sm text-slate-500">{new Date(checkin.checked_in_at).toLocaleDateString('pt-BR')}</span></div><p className="mt-1 text-sm text-slate-400">Condutor: {checkin.driver_name || checkin.driver?.name || 'Não identificado'}</p></div><ChevronRight className="size-5 self-center text-slate-500" /></button>) : <p className="italic text-slate-500">Nenhum registro de histórico encontrado.</p>}</div>{selected && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4" role="dialog" aria-modal="true"><section className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl border border-slate-700 bg-slate-900 p-6 shadow-2xl"><div className="flex justify-between"><div><h2 className="text-xl font-bold text-white">Detalhes dos apontamentos</h2><p className="mt-1 text-sm text-slate-400">{new Date(selected.checked_in_at).toLocaleString('pt-BR')}</p></div><button onClick={() => setSelected(null)} className="p-2 text-slate-400"><X className="size-5" /></button></div><div className="mt-6 space-y-4 text-sm"><p><span className="text-slate-500">Condutor:</span> <span className="text-slate-200">{selected.driver_name || selected.driver?.name || 'Não identificado'}</span></p><p><span className="text-slate-500">Odômetro:</span> <span className="text-slate-200">{selected.odometer} km</span></p><p><span className="text-slate-500">Combustível:</span> <span className="text-slate-200">{selected.fuel_level || 'Não informado'}</span></p><p><span className="text-slate-500">Situação:</span> <span className="text-slate-200">{selected.has_issues ? `${selected.resolved ? 'Resolvido' : 'Pendente'}${selected.alert_level ? ` · ${selected.alert_level}` : ''}` : 'Sem apontamentos'}</span></p>{(selected.return_notes || selected.repair_notes) && <div className="rounded-lg border border-amber-500/20 bg-amber-500/10 p-4 text-amber-100"><AlertTriangle className="mb-2 size-5 text-amber-400" />{selected.return_notes || selected.repair_notes}</div>}</div><button onClick={() => setSelected(null)} className="mt-6 rounded-lg bg-slate-800 px-4 py-2 text-white">Fechar</button></section></div>}</div>; }
